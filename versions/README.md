# Version Archive

This folder stores source snapshots from before the active project was renamed to `3rdPersonCamFlip`.

- `alpha/0.01/` contains the old `ThirdPersonCamFix - 0.0.4` source snapshot. This is the first alpha prototype.
- `pre-alpha/0.0.2/` contains the old `ThirdPersonCamFix - 0.0.2` source snapshot.
- `pre-alpha/0.0.3/` contains the old `ThirdPersonCamFix - 0.0.3` source snapshot.
- `pre-alpha/broken-1/` and `pre-alpha/broken-2/` contain earlier broken prototypes kept for reference.

The active source lives in `src/3rdPersonCamFlip/`.
